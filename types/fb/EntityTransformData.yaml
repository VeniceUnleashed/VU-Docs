name: EntityTransformData
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new EntityTransformData frostbite instance.
  -
    description: Creates a new EntityTransformData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to EntityTransformData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to EntityTransformData.

properties:
  entityTransforms:
    type: LinearTransform
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the EntityTransformData type.

