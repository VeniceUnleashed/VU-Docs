name: GainFaderNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new GainFaderNodeData frostbite instance.
  -
    description: Creates a new GainFaderNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to GainFaderNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to GainFaderNodeData.
  -
    description: Casts an instance of type DataContainer to GainFaderNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to GainFaderNodeData.

properties:
  inValue:
    type: AudioGraphNodePort
  start:
    type: AudioGraphNodePort
  startTime:
    type: AudioGraphNodePort
  fadeTime:
    type: AudioGraphNodePort
  amplitude:
    type: AudioGraphNodePort
  out:
    type: AudioGraphNodePort
  fadeType:
    type: GainFaderFadeType
  plugin:
    type: SoundGraphPluginRef

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the GainFaderNodeData type.

