name: UIManDownCompData
type: class
inherits: UIComponentData

constructors:
  -
    description: Creates a new UIManDownCompData frostbite instance.
  -
    description: Creates a new UIManDownCompData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type UIComponentData to UIManDownCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: UIComponentData
        description: The instance to cast to UIManDownCompData.
  -
    description: Casts an instance of type Asset to UIManDownCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to UIManDownCompData.
  -
    description: Casts an instance of type DataContainer to UIManDownCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to UIManDownCompData.

properties:
  delayManDownTextTime:
    type: float

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the UIManDownCompData type.

