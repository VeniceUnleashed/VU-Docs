name: TargetCameraData
type: class
inherits: CameraData

constructors:
  -
    description: Creates a new TargetCameraData frostbite instance.
  -
    description: Creates a new TargetCameraData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type CameraData to TargetCameraData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: CameraData
        description: The instance to cast to TargetCameraData.
  -
    description: Casts an instance of type GameObjectData to TargetCameraData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameObjectData
        description: The instance to cast to TargetCameraData.
  -
    description: Casts an instance of type GameDataContainer to TargetCameraData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameDataContainer
        description: The instance to cast to TargetCameraData.
  -
    description: Casts an instance of type DataContainer to TargetCameraData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to TargetCameraData.

properties:
  hud:
    type: HudData
  uiParts:
    type: UIPartData
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the TargetCameraData type.

