name: MixerNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new MixerNodeData frostbite instance.
  -
    description: Creates a new MixerNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to MixerNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to MixerNodeData.
  -
    description: Casts an instance of type DataContainer to MixerNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MixerNodeData.

properties:
  entries:
    type: MixerEntry
    array: true
  out:
    type: AudioGraphNodePort

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MixerNodeData type.

