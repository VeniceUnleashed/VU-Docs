name: ImpulseResponseAsset
type: class
inherits: SoundDataAsset

constructors:
  -
    description: Creates a new ImpulseResponseAsset frostbite instance.
  -
    description: Creates a new ImpulseResponseAsset frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type SoundDataAsset to ImpulseResponseAsset. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: SoundDataAsset
        description: The instance to cast to ImpulseResponseAsset.
  -
    description: Casts an instance of type Asset to ImpulseResponseAsset. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to ImpulseResponseAsset.
  -
    description: Casts an instance of type DataContainer to ImpulseResponseAsset. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to ImpulseResponseAsset.

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the ImpulseResponseAsset type.

