name: VeniceExplosionEntityData
type: class
inherits: ExplosionEntityData

constructors:
  -
    description: Creates a new VeniceExplosionEntityData frostbite instance.
  -
    description: Creates a new VeniceExplosionEntityData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type ExplosionEntityData to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: ExplosionEntityData
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type GameEntityData to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameEntityData
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type SpatialEntityData to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: SpatialEntityData
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type EntityData to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: EntityData
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type GameObjectData to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameObjectData
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type GameDataContainer to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameDataContainer
        description: The instance to cast to VeniceExplosionEntityData.
  -
    description: Casts an instance of type DataContainer to VeniceExplosionEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VeniceExplosionEntityData.

properties:
  unspottableTime:
    type: float
  unspotsOnExplode:
    type: bool
  spotsOnExplode:
    type: bool
  isCausingSuppression:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VeniceExplosionEntityData type.

