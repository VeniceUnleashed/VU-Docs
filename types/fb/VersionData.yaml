name: VersionData
type: class
inherits: Asset

constructors:
  -
    description: Creates a new VersionData frostbite instance.
  -
    description: Creates a new VersionData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type Asset to VersionData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to VersionData.
  -
    description: Casts an instance of type DataContainer to VersionData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VersionData.

properties:
  disclaimer:
    type: string
  version:
    type: int
  dateTime:
    type: string
  branchId:
    type: string
  gameName:
    type: string

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VersionData type.

