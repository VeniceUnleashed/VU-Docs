name: VoiceOverLanguageRoot
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new VoiceOverLanguageRoot frostbite instance.
  -
    description: Creates a new VoiceOverLanguageRoot frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to VoiceOverLanguageRoot. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VoiceOverLanguageRoot.

properties:
  language:
    type: AudioLanguage
    nullable: true
  path:
    type: string

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VoiceOverLanguageRoot type.

