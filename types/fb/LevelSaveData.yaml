name: LevelSaveData
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new LevelSaveData frostbite instance.
  -
    description: Creates a new LevelSaveData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to LevelSaveData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to LevelSaveData.

properties:
  saveSize:
    type: int
  saveNames:
    type: string
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the LevelSaveData type.

