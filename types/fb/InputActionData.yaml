name: InputActionData
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new InputActionData frostbite instance.
  -
    description: Creates a new InputActionData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to InputActionData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to InputActionData.

properties:
  isAnalog:
    type: bool
  negateValue:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the InputActionData type.

