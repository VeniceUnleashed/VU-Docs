name: QueryGetNode
type: class
inherits: UINodeData

constructors:
  -
    description: Creates a new QueryGetNode frostbite instance.
  -
    description: Creates a new QueryGetNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type UINodeData to QueryGetNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: UINodeData
        description: The instance to cast to QueryGetNode.
  -
    description: Casts an instance of type DataContainer to QueryGetNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to QueryGetNode.

properties:
  inValue:
    type: UINodePort
    nullable: true
  out:
    type: UINodePort
    nullable: true
  dataSource:
    type: UIDataSourceInfo

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the QueryGetNode type.

