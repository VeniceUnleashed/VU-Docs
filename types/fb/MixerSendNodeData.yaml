name: MixerSendNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new MixerSendNodeData frostbite instance.
  -
    description: Creates a new MixerSendNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to MixerSendNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to MixerSendNodeData.
  -
    description: Casts an instance of type DataContainer to MixerSendNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MixerSendNodeData.

properties:
  entries:
    type: MixerSendEntry
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MixerSendNodeData type.

