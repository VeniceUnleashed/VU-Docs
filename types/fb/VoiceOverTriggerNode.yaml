name: VoiceOverTriggerNode
type: class
inherits: VoiceOverStructureNode

constructors:
  -
    description: Creates a new VoiceOverTriggerNode frostbite instance.
  -
    description: Creates a new VoiceOverTriggerNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type VoiceOverStructureNode to VoiceOverTriggerNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverStructureNode
        description: The instance to cast to VoiceOverTriggerNode.
  -
    description: Casts an instance of type VoiceOverNode to VoiceOverTriggerNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverNode
        description: The instance to cast to VoiceOverTriggerNode.
  -
    description: Casts an instance of type DataContainer to VoiceOverTriggerNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VoiceOverTriggerNode.

properties:
  parameters:
    type: VoiceOverTriggerParameter
    array: true
  delay:
    type: float
  event:
    type: VoiceOverEvent
    nullable: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VoiceOverTriggerNode type.

