name: MortarStrikeWeaponData
type: class
inherits: WeaponData

constructors:
  -
    description: Creates a new MortarStrikeWeaponData frostbite instance.
  -
    description: Creates a new MortarStrikeWeaponData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type WeaponData to MortarStrikeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: WeaponData
        description: The instance to cast to MortarStrikeWeaponData.
  -
    description: Casts an instance of type ToolData to MortarStrikeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: ToolData
        description: The instance to cast to MortarStrikeWeaponData.
  -
    description: Casts an instance of type DataContainer to MortarStrikeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MortarStrikeWeaponData.

properties:
  holdingTolerance:
    type: float
  maxStrikeDistance:
    type: float
  strikeRadius:
    type: float
  maxRandomSpawnHeight:
    type: float

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MortarStrikeWeaponData type.

