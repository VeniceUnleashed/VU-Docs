name: RenderVolumeEntityData
type: class
inherits: SpatialEntityData

constructors:
  -
    description: Creates a new RenderVolumeEntityData frostbite instance.
  -
    description: Creates a new RenderVolumeEntityData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type SpatialEntityData to RenderVolumeEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: SpatialEntityData
        description: The instance to cast to RenderVolumeEntityData.
  -
    description: Casts an instance of type EntityData to RenderVolumeEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: EntityData
        description: The instance to cast to RenderVolumeEntityData.
  -
    description: Casts an instance of type GameObjectData to RenderVolumeEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameObjectData
        description: The instance to cast to RenderVolumeEntityData.
  -
    description: Casts an instance of type GameDataContainer to RenderVolumeEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: GameDataContainer
        description: The instance to cast to RenderVolumeEntityData.
  -
    description: Casts an instance of type DataContainer to RenderVolumeEntityData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to RenderVolumeEntityData.

properties:
  userMasks:
    type: Vec4
  shader:
    type: SurfaceShaderInstanceDataStruct
  transformType:
    type: RenderVolumeTransformType
  enabled:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the RenderVolumeEntityData type.

