name: DataToggleNode
type: class
inherits: UINodeData

constructors:
  -
    description: Creates a new DataToggleNode frostbite instance.
  -
    description: Creates a new DataToggleNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type UINodeData to DataToggleNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: UINodeData
        description: The instance to cast to DataToggleNode.
  -
    description: Casts an instance of type DataContainer to DataToggleNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to DataToggleNode.

properties:
  inValue:
    type: UINodePort
    nullable: true
  out:
    type: UINodePort
    nullable: true
  dataSource:
    type: UIDataSourceInfo

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the DataToggleNode type.

