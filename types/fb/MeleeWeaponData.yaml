name: MeleeWeaponData
type: class
inherits: WeaponData

constructors:
  -
    description: Creates a new MeleeWeaponData frostbite instance.
  -
    description: Creates a new MeleeWeaponData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type WeaponData to MeleeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: WeaponData
        description: The instance to cast to MeleeWeaponData.
  -
    description: Casts an instance of type ToolData to MeleeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: ToolData
        description: The instance to cast to MeleeWeaponData.
  -
    description: Casts an instance of type DataContainer to MeleeWeaponData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MeleeWeaponData.

properties:
  useCannedAnimation:
    type: bool
  useSphereDamage:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MeleeWeaponData type.

