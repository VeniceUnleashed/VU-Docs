name: SoldierEmoteNetworkedMessageMapping
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new SoldierEmoteNetworkedMessageMapping frostbite instance.
  -
    description: Creates a new SoldierEmoteNetworkedMessageMapping frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to SoldierEmoteNetworkedMessageMapping. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to SoldierEmoteNetworkedMessageMapping.

properties:
  emote:
    type: SoldierEmote
  messages:
    type: UIMessageType
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the SoldierEmoteNetworkedMessageMapping type.

