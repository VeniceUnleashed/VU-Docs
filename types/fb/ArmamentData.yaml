name: ArmamentData
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new ArmamentData frostbite instance.
  -
    description: Creates a new ArmamentData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to ArmamentData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to ArmamentData.

properties:
  threatLength:
    type: int
  turretData:
    type: TurretData
    nullable: true
  isAntiAircraft:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the ArmamentData type.

