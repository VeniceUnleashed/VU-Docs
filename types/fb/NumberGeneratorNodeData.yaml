name: NumberGeneratorNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new NumberGeneratorNodeData frostbite instance.
  -
    description: Creates a new NumberGeneratorNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to NumberGeneratorNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to NumberGeneratorNodeData.
  -
    description: Casts an instance of type DataContainer to NumberGeneratorNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to NumberGeneratorNodeData.

properties:
  trigger:
    type: AudioGraphNodePort
  min:
    type: float
  max:
    type: float
  mode:
    type: NumberGeneratorMode
  y:
    type: AudioGraphNodePort

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the NumberGeneratorNodeData type.

