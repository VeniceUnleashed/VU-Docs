name: UIChatCompData
type: class
inherits: UIComponentData

constructors:
  -
    description: Creates a new UIChatCompData frostbite instance.
  -
    description: Creates a new UIChatCompData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type UIComponentData to UIChatCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: UIComponentData
        description: The instance to cast to UIChatCompData.
  -
    description: Casts an instance of type Asset to UIChatCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to UIChatCompData.
  -
    description: Casts an instance of type DataContainer to UIChatCompData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to UIChatCompData.

properties:
  settings:
    type: ChatSettings
    nullable: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the UIChatCompData type.

