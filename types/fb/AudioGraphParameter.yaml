name: AudioGraphParameter
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new AudioGraphParameter frostbite instance.
  -
    description: Creates a new AudioGraphParameter frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to AudioGraphParameter. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to AudioGraphParameter.

properties:
  defaultValue:
    type: float
  nameHash:
    type: int
  valueIndex:
    type: int

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the AudioGraphParameter type.

