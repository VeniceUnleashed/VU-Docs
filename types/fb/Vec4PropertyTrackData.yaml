name: Vec4PropertyTrackData
type: class
inherits: SimplePropertyTrackData

constructors:
  -
    description: Creates a new Vec4PropertyTrackData frostbite instance.
  -
    description: Creates a new Vec4PropertyTrackData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type SimplePropertyTrackData to Vec4PropertyTrackData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: SimplePropertyTrackData
        description: The instance to cast to Vec4PropertyTrackData.
  -
    description: Casts an instance of type PropertyTrackData to Vec4PropertyTrackData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: PropertyTrackData
        description: The instance to cast to Vec4PropertyTrackData.
  -
    description: Casts an instance of type DataContainer to Vec4PropertyTrackData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to Vec4PropertyTrackData.

properties:
  values:
    type: Vec4
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the Vec4PropertyTrackData type.

