name: TimingViewCategories
type: class
inherits: Asset

constructors:
  -
    description: Creates a new TimingViewCategories frostbite instance.
  -
    description: Creates a new TimingViewCategories frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type Asset to TimingViewCategories. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to TimingViewCategories.
  -
    description: Casts an instance of type DataContainer to TimingViewCategories. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to TimingViewCategories.

properties:
  timingCategories:
    type: TimingViewCategory
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the TimingViewCategories type.

