name: VoiceOverProbabilityNode
type: class
inherits: VoiceOverExpressionNode

constructors:
  -
    description: Creates a new VoiceOverProbabilityNode frostbite instance.
  -
    description: Creates a new VoiceOverProbabilityNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type VoiceOverExpressionNode to VoiceOverProbabilityNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverExpressionNode
        description: The instance to cast to VoiceOverProbabilityNode.
  -
    description: Casts an instance of type VoiceOverNode to VoiceOverProbabilityNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverNode
        description: The instance to cast to VoiceOverProbabilityNode.
  -
    description: Casts an instance of type DataContainer to VoiceOverProbabilityNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VoiceOverProbabilityNode.

properties:
  falseValue:
    type: VoiceOverValue
    nullable: true
  trueValue:
    type: VoiceOverValue
    nullable: true
  probability:
    type: float

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VoiceOverProbabilityNode type.

