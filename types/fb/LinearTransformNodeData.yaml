name: LinearTransformNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new LinearTransformNodeData frostbite instance.
  -
    description: Creates a new LinearTransformNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to LinearTransformNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to LinearTransformNodeData.
  -
    description: Casts an instance of type DataContainer to LinearTransformNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to LinearTransformNodeData.

properties:
  x:
    type: AudioGraphNodePort
  k:
    type: AudioGraphNodePort
  m:
    type: AudioGraphNodePort
  y:
    type: AudioGraphNodePort

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the LinearTransformNodeData type.

