name: InstanceOutputNode
type: class
inherits: UINodeData

constructors:
  -
    description: Creates a new InstanceOutputNode frostbite instance.
  -
    description: Creates a new InstanceOutputNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type UINodeData to InstanceOutputNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: UINodeData
        description: The instance to cast to InstanceOutputNode.
  -
    description: Casts an instance of type DataContainer to InstanceOutputNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to InstanceOutputNode.

properties:
  inValue:
    type: UINodePort
    nullable: true
  id:
    type: int
  destroyGraph:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the InstanceOutputNode type.

