name: VoiceOverStructureConnection
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new VoiceOverStructureConnection frostbite instance.
  -
    description: Creates a new VoiceOverStructureConnection frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to VoiceOverStructureConnection. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VoiceOverStructureConnection.

properties:
  targetNode:
    type: VoiceOverStructureNode
    nullable: true
  targetInput:
    type: VoiceOverRelationshipInput
    nullable: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VoiceOverStructureConnection type.

