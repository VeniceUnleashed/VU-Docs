name: VoiceOverConstantNode
type: class
inherits: VoiceOverExpressionNode

constructors:
  -
    description: Creates a new VoiceOverConstantNode frostbite instance.
  -
    description: Creates a new VoiceOverConstantNode frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type VoiceOverExpressionNode to VoiceOverConstantNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverExpressionNode
        description: The instance to cast to VoiceOverConstantNode.
  -
    description: Casts an instance of type VoiceOverNode to VoiceOverConstantNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: VoiceOverNode
        description: The instance to cast to VoiceOverConstantNode.
  -
    description: Casts an instance of type DataContainer to VoiceOverConstantNode. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VoiceOverConstantNode.

properties:
  value:
    type: VoiceOverValue
    nullable: true
  source:
    type: VoiceOverConstantValue
    nullable: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VoiceOverConstantNode type.

