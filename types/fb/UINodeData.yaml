name: UINodeData
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new UINodeData frostbite instance.
  -
    description: Creates a new UINodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to UINodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to UINodeData.

properties:
  name:
    type: string
  parentGraph:
    type: Asset
    nullable: true
  isRootNode:
    type: bool
  parentIsScreen:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the UINodeData type.

