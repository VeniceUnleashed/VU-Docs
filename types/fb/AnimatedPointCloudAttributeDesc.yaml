name: AnimatedPointCloudAttributeDesc
type: class

constructors:
  -
    description: Creates a new AnimatedPointCloudAttributeDesc frostbite instance.
  -
    description: Creates a new AnimatedPointCloudAttributeDesc frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.

properties:
  usage:
    type: PointCloudAttributeUsage
  iFrameQuantization:
    type: PointCloudAttributeQuantization
  dFrameQuantization:
    type: PointCloudAttributeQuantization

methods:
  -
    name: Clone
    description: Creates a shallow-copy clone of this structure, which is essentially the equivalent of creating a new structure of the same type and assigning the values of this structure to all of its properties. Any properties that contain structure types (eg. Vec3) will be cloned when assigning, while properties that contain instance types (eg. DataContainer) will be referencing the same instance.
    returns:
      type: AnimatedPointCloudAttributeDesc
      description: The newly created structure.

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the AnimatedPointCloudAttributeDesc type.

