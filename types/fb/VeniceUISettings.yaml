name: VeniceUISettings
type: class
inherits: SystemSettings

constructors:
  -
    description: Creates a new VeniceUISettings frostbite instance.
  -
    description: Creates a new VeniceUISettings frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type SystemSettings to VeniceUISettings. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: SystemSettings
        description: The instance to cast to VeniceUISettings.
  -
    description: Casts an instance of type DataContainer to VeniceUISettings. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to VeniceUISettings.

properties:
  metaDataAssetPath:
    type: string
  getStatsInOnlineFlow:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the VeniceUISettings type.

