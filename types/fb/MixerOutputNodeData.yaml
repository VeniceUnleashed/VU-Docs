name: MixerOutputNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new MixerOutputNodeData frostbite instance.
  -
    description: Creates a new MixerOutputNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to MixerOutputNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to MixerOutputNodeData.
  -
    description: Casts an instance of type DataContainer to MixerOutputNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MixerOutputNodeData.

properties:
  entries:
    type: MixerOutputEntry
    array: true

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MixerOutputNodeData type.

