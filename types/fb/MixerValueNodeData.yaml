name: MixerValueNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new MixerValueNodeData frostbite instance.
  -
    description: Creates a new MixerValueNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to MixerValueNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to MixerValueNodeData.
  -
    description: Casts an instance of type DataContainer to MixerValueNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to MixerValueNodeData.

properties:
  out:
    type: AudioGraphNodePort
  defaultValue:
    type: float

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the MixerValueNodeData type.

