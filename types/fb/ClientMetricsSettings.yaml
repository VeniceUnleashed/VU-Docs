name: ClientMetricsSettings
type: class
inherits: DataContainer

constructors:
  -
    description: Creates a new ClientMetricsSettings frostbite instance.
  -
    description: Creates a new ClientMetricsSettings frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type DataContainer to ClientMetricsSettings. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to ClientMetricsSettings.

properties:
  enabled:
    type: bool

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the ClientMetricsSettings type.

