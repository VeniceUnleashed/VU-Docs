name: AiffWriterNodeData
type: class
inherits: AudioGraphNodeData

constructors:
  -
    description: Creates a new AiffWriterNodeData frostbite instance.
  -
    description: Creates a new AiffWriterNodeData frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type AudioGraphNodeData to AiffWriterNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: AudioGraphNodeData
        description: The instance to cast to AiffWriterNodeData.
  -
    description: Casts an instance of type DataContainer to AiffWriterNodeData. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to AiffWriterNodeData.

properties:
  inValue:
    type: AudioGraphNodePort
  start:
    type: AudioGraphNodePort
  stop:
    type: AudioGraphNodePort
  plugin:
    type: SoundGraphPluginRef
  fileName:
    type: string

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the AiffWriterNodeData type.

