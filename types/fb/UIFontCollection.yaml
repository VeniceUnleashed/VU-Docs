name: UIFontCollection
type: class
inherits: Asset

constructors:
  -
    description: Creates a new UIFontCollection frostbite instance.
  -
    description: Creates a new UIFontCollection frostbite instance and assigns it the provided Guid.
    params:
      guid:
        type: Guid
        description: The Guid to assign to the newly created instance.
  -
    description: Casts an instance of type Asset to UIFontCollection. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: Asset
        description: The instance to cast to UIFontCollection.
  -
    description: Casts an instance of type DataContainer to UIFontCollection. Will throw an error when trying to cast from an unsupported type.
    params:
      other:
        type: DataContainer
        description: The instance to cast to UIFontCollection.

properties:
  language:
    type: LanguageFormat
  textDatabase:
    type: UITextDatabase
    nullable: true
  fonts:
    type: UIFontAsset
    array: true
  bundleKind:
    type: ResourceBundleKind

static:
  typeInfo:
    type: TypeInformation
    description: The type information for the UIFontCollection type.

