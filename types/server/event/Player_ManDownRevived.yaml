name: "Player:ManDownRevived"
type: event
description: This is used for coop revivals only

params:
  player:
    type: Player
  reviver:
    type: Player
    nullable: true
  isAdrenalineRevive:
    type: bool